[project]
name = "routebook-agent"
version = "0.1.0"
description = "RouteBook Agent learning prototypes"
requires-python = ">=3.12,<3.15"
dependencies = [
    "anthropic>=0.75,<1.0",
    "httpx>=0.28,<1.0",
    "langgraph>=1.0,<2.0",
    "python-dotenv>=1.0,<2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<10.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
